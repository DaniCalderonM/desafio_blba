<p style="color: green"><%= notice %></p>
<div class="bg-light p-2 text-dark bg-opacity-75 col-4 pb-4">
  <strong>Nombre: </strong><%= @client.name %>
  <br>
    <strong>Edad: </strong><%= @client.age %>
    <br>
    <% if @client.movies.any? %>
    <strong>Pelicula/s arrendada/s actual/es: </strong>
    <% @client.movies.each do |m| %>
  <p> ~<%= m.name %></p>
     <% end %>
    <% else %>
    <strong>Pelicula arrendada actual: </strong> Actualmente no tiene peliculas arrendadas
    <% end %>
   
</div>
<div>
  <%= link_to "Editar este Cliente", edit_client_path(@client), class: "btn btn-dark" %> |
  <%= link_to "Volver a Clientes", clients_path, class: "btn btn-dark" %>

  <%= button_to "Borrar este Cliente", @client, method: :delete %>
</div>
